{
  "schema_version": "msnlib-validation/output-schema-v2",
  "artifacts": {
    "code_manifest.json": "SHA-256 map of every benchmark, Hybrid, and statically resolved local MS2LDA dependency affecting the frozen run.",
    "completion_manifest.jsonl": "One validation/test row with immutable observed and completion physical-peak indices.",
    "chemical_inference/features/manifest.json": "Full-test-spectrum DreaMS cache provenance, resumable extraction state, label-withholding flags, timing, memory, and hashes for chemical evaluation only.",
    "chemical_inference/seed_<seed>/<method>/complete.json": "Full-spectrum held-out topic-mixture hashes and calibration diagnostics for every chemical-inference arm.",
    "chemical_inference/complete.json": "Proof that full-spectrum, label-withheld chemical inference completed for every frozen seed and method.",
    "config.resolved.json": "Canonical frozen scientific configuration.",
    "core/seed_<seed>/hybrid/checkpoint_resume_audit.json": "Selected or rejected Hybrid checkpoint generations and their frozen context hash.",
    "core/seed_<seed>/hybrid/checkpoints/checkpoint-*.json": "SHA-256 sidecar for one atomic discovery or encoder epoch checkpoint; only the newest configured generations are retained.",
    "core/seed_<seed>/hybrid/checkpoints/checkpoint-*.pt": "External weights-only resumable state containing topics, gamma, optimizers, RNG, counters, and histories but no duplicated spectra or feature cache.",
    "core/seed_<seed>/hybrid/continuation_import.json": "Source and target protocol hashes plus checkpoint migration audit for a training-only max-epoch continuation.",
    "core_artifact_reuse.json": "Hash inventory and source-protocol provenance when a derived run reuses completed DreaMS and Tomotopy artifacts or an unfinished Hybrid checkpoint.",
    "core/complete.json": "Proof that every required seed and both topic-model methods completed.",
    "core/seed_<seed>/<method>/complete.json": "Per-method metrics, initial/final alpha summaries, execution settings, and SHA-256 digests for every consumed beta, theta, and finalized model artifact.",
    "environment.json": "Python, operating-system, hardware, and dependency provenance at freeze time.",
    "execution_log.jsonl": "Append-only exact stage commands, executables, Conda environments, timestamps, and status.",
    "features/manifest.json": "DreaMS cache dimensions, provenance, leakage flags, timing, memory, and output hashes.",
    "features/checkpoint_generations/generation-*/": "Atomic word-pool sums, weights, counters, completed-row state and SHA-256 manifest; the newest two verified generations are retained.",
    "freeze_summary.json": "Source parsing, split, vocabulary, document-completion, and zero-leak audit counts.",
    "input_manifest.json": "Absolute input locations, byte sizes, and SHA-256 digests.",
    "mag/complete.json": "Proof of all per-seed MAG arms, filtered-index provenance, raw-DreaMS completion, and legacy environment.",
    "mag/seed_<seed>/<method>/topics.jsonl": "Per-topic and per-inference-arm MAG retrieval, clustering, optimization eligibility, dominant-topic and frozen-threshold associations, compound-balanced primary and spectrum-weighted sensitivity SOS under both denominator definitions, coverage, and error rows.",
    "model_assignments.json": "Chemistry-free spectrum-ID to partition map consumed by model workers.",
    "pipeline_state.json": "Durable status, commands, completion timestamps, and output hashes for the unattended core-to-report pipeline.",
    "protocol.lock.json": "Self-hashed lock over configuration, manifests, source, command, branch, commit, dirty state, prior test inspection, and any audited parent derivation.",
    "tomotopy_artifact_reuse.json": "Hash inventory and source-protocol provenance for the narrowly scoped import of an unchanged Tomotopy core model; features and Hybrid artifacts are never imported.",
    "core/seed_<seed>/tomotopy/checkpoints/checkpoint-*.bin": "Atomic Tomotopy training generation; two verified generations are retained for fallback.",
    "core/seed_<seed>/tomotopy/checkpoints/checkpoint-*.json": "Configuration context and SHA-256 sidecar for one Tomotopy checkpoint generation.",
    "report/mag_agreement.jsonl": "Same-seed matched MACCS consensus agreement; similarity, not correctness.",
    "report/mag_per_seed.jsonl": "MAG optimization coverage, compound-balanced primary and spectrum-weighted sensitivity SOS under both denominator definitions, association counts, failures, runtime, memory, and exclusion audit for every method, inference arm, association mode, and seed.",
    "report/manuscript_results.tex": "Generated seed-median or explicitly single-seed-indicative table for transcription into the method document.",
    "report/per_seed_metrics.csv": "One row per required seed and inference arm with predictive, convergence, alpha, diversity, timing, and memory metrics.",
    "report/summary.json": "Complete per-seed and aggregate report including limitations and raw-DreaMS baseline.",
    "report/topic_similarity.jsonl": "Optimal topic matches within methods across seeds when available and between methods within seeds.",
    "split_manifest.jsonl": "One source-spectrum row with partition and chemistry fields for independent leakage auditing.",
    "vocabulary.json": "Sorted vocabulary constructed from training spectra only."
  },
  "null_policy": "JSON null denotes an unavailable or inapplicable metric; NaN and infinity are forbidden.",
  "result_scope": "Generated run artifacts stay outside Git; smoke outputs are software validation only."
}
