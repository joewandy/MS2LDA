\documentclass[10pt,a4paper]{article}
\usepackage[margin=2.0cm]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}
\usepackage{enumitem}
\definecolor{accent}{HTML}{1D4ED8}
\hypersetup{
  colorlinks=true,
  linkcolor=accent,
  urlcolor=accent,
  citecolor=accent,
  pdftitle={Routing-Informed Sparse ETM for Neural MS2LDA}
}
\setlist{nosep,leftmargin=*}
\newcommand{\softmax}{\operatorname{softmax}}
\newcommand{\entmax}{\operatorname{entmax}_{1.5}}
\newcommand{\normalize}{\operatorname{normalize}}
\newcommand{\centerop}{\operatorname{center}}

\title{Routing-Informed Sparse ETM for Neural MS2LDA:\\
Validation Checkpoint and Model Selection}
\author{}
\date{30 August 2026}

\begin{document}
\maketitle

\begin{abstract}
Mass2Motif discovery requires more than good held-out likelihood. A useful model
must retain a broad, non-redundant topic inventory and assign short tandem mass
spectra strongly enough for chemical evaluation. Canonical and balanced Embedded
Topic Models (ETMs) retained reasonable likelihood but produced diffuse document
mixtures, while a sparse ETM using entmax alone starved most topics.

We therefore retained the published ETM generator and added two bounded inference
adaptations: top-2 contextual evidence from observed spectral words and published
alpha-entmax 1.5. The resulting model has only one additional learned scalar. On
the locked seed-42, $K=1000$ MS$^{n}$Lib validation split it produces 445
evaluable and 289 useful motifs, compared with 408/265 for the private M1
reference and 206/138 for Tomotopy. Its median spectrum uses 3.70 effective
topics, 828 topics win at least one validation spectrum, and no catastrophic
duplicate component appears.

The model does not dominate every metric. It produces 803 optimized motifs,
mean SOS 0.647153 and completion NLL 9.542924, versus 884, 0.658079 and 8.974140
for M1. Three conservative M1-relative gates are therefore false and candidate
test remains locked. The correct scientific conclusion is a strong near-pass and
a viable paper-facing baseline: the principal discovery breadth, sparsity and
inventory failures are repaired, while residual chemical-quality and likelihood
trade-offs remain explicit. The next priority is reproducibility and real
training-seed stability, not reconstruction of the private M1 architecture.
\end{abstract}

\section{Scientific objective}

MS2LDA represents tandem mass spectra as documents, fragment and neutral-loss
tokens as words, and recurrent co-fragmentation patterns as Mass2Motifs
\cite{vanderhooft2016}. At $K=1000$, a useful model must simultaneously:
\begin{enumerate}
  \item predict held-out spectral evidence;
  \item retain many distinct topic-word distributions;
  \item assign each short spectrum to only a small number of motifs;
  \item produce enough confident spectrum-topic associations for MAG/SOS; and
  \item retain chemically useful motifs under leakage control.
\end{enumerate}

Likelihood alone can hide major failures. Topics may be starved, duplicated or
nearly uniform, and a spectrum may spread probability over dozens of topics even
when completion NLL is good. Earlier ETM experiments reproduced both failure
classes. This study asks whether a recognizable ETM base can be adapted to the
short MS/MS document regime without rebuilding the private M1 system.

\section{Evidence and metric contract}

The experiment uses the positive-mode MS$^{n}$Lib assets from Zenodo record
20179680 \cite{brungs2025,msnlibdata}. The locked scaffold/compound-disjoint split
contains 27,222 training and 3,889 validation spectra. Vocabulary and
48-dimensional SGNS features are constructed from training spectra only.
Physical peak groups are divided as units for document completion, preventing a
fragment and its corresponding neutral loss from leaking across observed and
withheld halves.

MAG retrieves leakage-filtered reference spectra and optimizes motif annotations.
The frozen validation membership threshold is $\theta_{dk}\geq0.5$. The primary
metrics are:
\begin{itemize}
  \item \textbf{optimized motifs}: topics for which MAG produces at least one
  optimized annotation feature;
  \item \textbf{evaluable motifs}: optimized topics associated with at least one
  unique validation molecule;
  \item \textbf{SOS}: annotation-fingerprint containment in a molecule
  fingerprint, averaged over unique molecules and then over topics;
  \item \textbf{useful motifs}: evaluable topics with SOS $\geq0.6$; and
  \item \textbf{completion NLL}: negative log-likelihood per in-vocabulary
  withheld token under $\theta\beta$, where lower is better.
\end{itemize}

Candidate selection used training plus validation only. Candidate test theta,
completion, MAG, SOS and result files were not loaded, calculated or inspected.
The committed access audit records this boundary explicitly.

\section{Routing-informed sparse ETM}

\subsection{Published ETM generator}

Let $\rho_w\in\mathbb{R}^{48}$ be the fixed train-only SGNS coordinate of word
$w$ and $\alpha_k\in\mathbb{R}^{48}$ the learned ETM coordinate of topic $k$.
The decoder is the ordinary embedding-space topic model \cite{dieng2020}, with
separate fragment and neutral-loss normalizations so that each channel receives
half of every topic's probability mass:
\begin{equation}
  \beta_{kw}=\begin{cases}
  \tfrac12\softmax_{v\in V_F}(\alpha_k^\top\rho_v), & w\in V_F,\\
  \tfrac12\softmax_{v\in V_L}(\alpha_k^\top\rho_v), & w\in V_L.
  \end{cases}
\end{equation}

The two-layer ETM encoder returns Gaussian parameters $\mu(x)$ and $\sigma(x)$
for normalized spectrum bag-of-words $x$. The variational family, standard-normal
analytic KL, raw pseudo-count multinomial reconstruction and optimizer remain
unchanged.

\subsection{Top-2 contextual posterior evidence}

For each non-zero word, the model combines its normalized SGNS vector with the
weighted leave-one-out mean of the other observed words. One learned scalar $c$
controls the context contribution:
\begin{equation}
  h_{dw}=\normalize\!\left(
  \normalize(\rho_w)+c\,\frac{\sum_vx_{dv}\rho_v-x_{dw}\normalize(\rho_w)}
  {\max(1-x_{dw},\epsilon)}\right).
\end{equation}

The contextual word is compared with the same normalized topic coordinates that
define $\beta$:
\begin{equation}
  s_{dwk}=\frac{h_{dw}^{\top}\normalize(\alpha_k)}{\tau_r},
  \qquad \tau_r=1.0.
\end{equation}
Only its two largest topic logits are retained (the top-2 route) and normalized
with a softmax. Their word-weighted sum yields one document evidence
distribution $r_d$.

The ordinary ETM posterior mean receives a bounded row-centred log offset:
\begin{align}
  \widetilde\mu(x_d)
    &=\mu(x_d)+\centerop\!\left[\log\left(r_d+\tfrac1K\right)\right],\\
  z_d&=\widetilde\mu(x_d)+\sigma(x_d)\odot\varepsilon,
       \qquad \varepsilon\sim\mathcal N(0,I),\\
  \theta_d&=\entmax(z_d),\\
  p(w\mid d)&=\sum_k\theta_{dk}\beta_{kw}.
\end{align}

The $1/K$ pseudocount bounds topics absent from the sparse route. Row centring
makes uniform evidence an exact no-op. Alpha-entmax 1.5 supplies exact zeros
\cite{peters2019}. Routing temperature and strength are fixed at 1.0. The learned
context scalar finished at 0.1772, and total parameters increase from 19,278,000
for balanced ETM to 19,278,001.

There is no nonlinear M1 router, document gate, Sinkhorn target, NPMI loss,
prototype-separation loss, alternating optimizer or temperature schedule. The
paper-facing claim is therefore ETM plus one explainable short-spectrum posterior
adaptation and a published sparse simplex mapping.

\section{Synthetic mechanism isolation}

Truth-known synthetic spectra used 18 planted motifs, 1--3 generating motifs per
spectrum, paired fragment/loss words, train-only SGNS, raw intensity pseudo-counts,
800 training and 160 validation spectra, and frozen seeds 11, 23 and 37.

\begin{table}[ht]
\centering
\small
\caption{K=36 synthetic means. Beta and theta columns are cosine recovery against
planted truth.}
\label{tab:synthetic}
\begin{tabular}{@{}lrrrrrr@{}}
\toprule
Formulation & NLL & Beta & Theta & Median eff. & Active & Unique top-1 \\
\midrule
Balanced ETM + softmax & 6.4710 & 0.3052 & 0.4868 & 3.59 & 8.0 & 6.7 \\
Balanced ETM + entmax & 6.5977 & 0.2566 & 0.3973 & 1.88 & 5.0 & 4.7 \\
Top-2 context + softmax & \textbf{6.2438} & 0.4423 & 0.7066 & 3.22 & 13.3 & 11.7 \\
\textbf{Top-2 context + entmax} & 6.2892 & \textbf{0.4648} & \textbf{0.7513} & \textbf{2.00} & \textbf{14.0} & \textbf{13.0} \\
\bottomrule
\end{tabular}
\end{table}

Entmax alone makes mixtures sparse but worsens topic starvation. Routing alone
improves recovery and breadth but remains too diffuse in a separate deliberately
diffuse stress condition. Their combination is complementary: routing supplies a
broad recoverable posterior signal, and entmax converts it into sparse mixtures.

At seed-11 K=128, the selected combination recovered all 18 planted motifs as
distinct top-1 topics. It achieved beta/theta recovery 0.6638/0.9556, top-motif
accuracy 0.9313, median 1.45 effective topics, median exact support 2 and NLL
6.1276. This high-overcompleteness result supported promotion of one fixed
configuration to real validation.

\section{Real \texorpdfstring{MS$^{n}$Lib}{MSnLib} validation}

\subsection{Discovery breadth relative to M1 and Tomotopy}

\begin{table}[ht]
\centering
\small
\caption{Locked seed-42 validation comparison. Bold marks the best value in each
row, not a global model winner.}
\label{tab:primary-comparison}
\begin{tabular}{@{}lrrr@{}}
\toprule
Metric & M1 & \textbf{Routing ETM} & Tomotopy \\
\midrule
Optimized motifs & \textbf{884} & 803 & 607 \\
Evaluable motifs & 408 & \textbf{445} & 206 \\
Useful motifs & 265 & \textbf{289} & 138 \\
Mean SOS & 0.658079 & 0.647153 & \textbf{0.676149} \\
Median SOS & 0.648864 & 0.657895 & \textbf{0.685450} \\
Completion NLL $\downarrow$ & \textbf{8.974140} & 9.542924 & 9.662228 \\
\bottomrule
\end{tabular}
\end{table}

Routing ETM is better on discovery breadth: it adds 37 evaluable and 24 useful
motifs over M1 and 239/151 over Tomotopy. It also has lower completion NLL than
Tomotopy. It is not uniformly better: M1 retains higher optimized coverage,
mean SOS and likelihood, while Tomotopy has higher SOS over a much smaller
evaluable inventory.

The SOS bands explain the apparent mean/median tension.

\begin{table}[ht]
\centering
\small
\caption{Validation SOS composition. Useful motifs are the high and intermediate
bands combined.}
\label{tab:sos-bands}
\begin{tabular}{@{}lrrr@{}}
\toprule
SOS band & M1 & Routing ETM & Difference \\
\midrule
High, $>0.8$ & 79 & 67 & -12 \\
Intermediate, $0.6$--$0.8$ & 186 & 222 & +36 \\
Low, $<0.6$ & 143 & 156 & +13 \\
Evaluable total & 408 & 445 & +37 \\
Useful total & 265 & 289 & +24 \\
\bottomrule
\end{tabular}
\end{table}

Routing ETM increases the intermediate-quality inventory rather than reporting
only a small high-scoring subset. Evaluable-to-optimized conversion is 55.4\%
versus M1's 46.2\%, and useful-to-optimized conversion is 36.0\% versus 30.0\%.
Its median SOS is also higher than M1's.

\subsection{Sparse spectra without global collapse}

\begin{table}[ht]
\centering
\small
\caption{Diagnostics distinguishing per-spectrum sparsity from global topic
collapse.}
\label{tab:inventory}
\begin{tabular}{@{}lr@{}}
\toprule
Routing ETM diagnostic & Value \\
\midrule
Median / mean effective topics per spectrum & 3.70 / 4.09 \\
Median / p95 / maximum exact support & 6 / 13 / 23 \\
Unique top-1 topics & 828 \\
Topics active above mean usage 0.0005 & 535 \\
Corpus-effective topics & 538.40 \\
Maximum mean topic usage & 0.0143 \\
Mean nearest-topic beta cosine & 0.1529 \\
Largest component at beta cosine $\geq0.999$ & 2 topics \\
Catastrophic duplicate component & no \\
\bottomrule
\end{tabular}
\end{table}

Balanced softmax ETM used 46.72 median effective topics and produced only
166 evaluable / 104 useful motifs. Entmax alone used 1.80 effective topics but
collapsed to 20 unique top-1 topics and 7 evaluable / 6 useful motifs. Routing
plus entmax fixes both failures simultaneously: sparse document mixtures no
longer arise by starving the global inventory.

\subsection{Formal gates and scientific interpretation}

The gates were frozen before scoring. Motif counts retain 95\% of M1, mean SOS
retains 99\%, and NLL may be at most 105\% of M1. Every gate had to be true for
the formal pass.

\begin{table}[ht]
\centering
\small
\caption{Frozen gate audit. The Boolean remains false even though the candidate
is scientifically useful.}
\label{tab:gates}
\begin{tabular}{@{}lrrrl@{}}
\toprule
Gate & Threshold & Candidate & Distance & Result \\
\midrule
Optimized motifs & $\geq840$ & 803 & -37 & fail \\
Evaluable motifs & $\geq388$ & 445 & +57 & pass \\
Useful motifs & $\geq252$ & 289 & +37 & pass \\
Mean SOS & $\geq0.651498$ & 0.647153 & -0.004345 & fail \\
Completion NLL & $\leq9.422847$ & 9.542924 & +0.120077 & fail \\
Finite/stable & required & yes & -- & pass \\
No catastrophic component & required & yes & -- & pass \\
\bottomrule
\end{tabular}
\end{table}

The mean-SOS miss is 0.67\% below its threshold and NLL is 1.27\% over its
threshold. Optimized coverage is 4.40\% below its gate. The all-gates value must
remain false for audit integrity, but it is misleading to translate that Boolean
into a claim that Routing ETM is generally worse than M1. The saved evidence
supports the status \emph{strong near-pass and current paper-facing baseline}.

\section{Historical alternatives and negative evidence}

The campaign tested one mechanism at a time and retained negative results:
\begin{itemize}
  \item canonical fixed-SGNS ETM had good completion but only 130 evaluable and
  79 useful motifs, with 43.79 median effective topics;
  \item fragment/loss balancing raised optimized coverage from 609 to 911 but
  did not repair evaluable/useful breadth or SOS;
  \item post-hoc temperature sharpening changed mixture scale but could not
  jointly preserve breadth, SOS and NLL;
  \item pooled projected models contained a 614-topic near-exact beta component;
  \item entmax without routing produced exact sparsity by starving the inventory;
  \item the maintained ECRTM Sinkhorn solver failed its convergence contract at
  real $K=1000$ and vocabulary 21,233, so no partial model was scored; and
  \item a reference short-document NSTM failed the paired high-K synthetic screen
  and was not promoted to MSnLib.
\end{itemize}

These results justify the selected posterior adaptation without claiming that
every future topic model must fail. They also show why another likelihood-only
comparison would be insufficient.

\section{Runtime and system load}

The frozen real model trained for 120 epochs in 876.8 seconds (14.6 minutes) on
an NVIDIA GeForce RTX 5070. Including final inference and evidence hashing, the
run completed in about 16 minutes. Steady-state training used approximately
83\% GPU utilization, 53~$^{\circ}$C and 116 W of a 250 W limit.

Peak PyTorch allocated/reserved CUDA memory was 0.821/1.053 GB. Process high-water
memory was 2.879 GB, with at least 12.43 GB system memory remaining. Deterministic
full-validation inference processed approximately 23,426 spectra per second. The
load was sustained but comfortably below GPU memory, thermal, power and system
memory limits.

\section{Limitations and robustness}

The main unresolved uncertainty is real training-seed stability. The synthetic
mechanism screen used three seeds, but the real Routing ETM result is one
deterministic training seed on one positive-mode data split. It establishes this
run, not the variance of repeated fitting or generalization to other instruments,
ionization modes, biological samples or topic counts.

MAG/SOS is a library-dependent chemical proxy. Higher evaluable breadth can
lower an unweighted mean by admitting additional borderline topics, which is why
counts, mean, median and SOS bands are reported together. Expert interpretation
and independent-dataset replication remain necessary.

M1 is retained as a locked private comparator and donor-ablation source, not as
the proposed publication architecture. Tomotopy remains the independent
production comparator. No claim is made that Routing ETM should replace the
production backend before stability and untouched-test review.

\section{Recommended next evidence}

The checkpoint should be preserved before further model changes. The next steps
are:
\begin{enumerate}
  \item verify every committed source/evidence hash and retained local artifact;
  \item repeat the unchanged Routing ETM on two additional real training seeds,
  using validation only and no tuning against the outcomes;
  \item if the optimized-coverage and mean-SOS gap reproduces, consider one
  predeclared train-derived positive-NPMI coherence experiment;
  \item stop that intervention if it reduces the present evaluable/useful
  breadth, sparse support or global inventory; and
  \item unlock candidate test only after an independent review freezes the
  method, checkpoint and success interpretation.
\end{enumerate}

Do not add M1's document gate, Sinkhorn balancing, prototype separation,
alternating optimizer or temperature schedule. Additional complexity now risks
making an already strong and explainable result harder to publish.

\section{Conclusion}

Routing-informed sparse ETM repairs the central failure of neural topic models
on short MS/MS documents: it achieves sparse per-spectrum mixtures without
collapsing or starving the global topic inventory. It exceeds both M1 and
Tomotopy on evaluable and useful validation motif counts, while retaining a
recognizable ETM generator and adding only one learned scalar.

The result is not uniform dominance. M1 retains better optimized coverage, mean
SOS and completion NLL; Tomotopy has higher SOS over fewer evaluable motifs. The
predeclared all-gates Boolean remains false and candidate test remains locked.
Nevertheless, the scientific outcome is positive: Routing ETM is the strongest
paper-facing discovery baseline produced by this campaign. Future work should
establish stability and preserve explainability rather than treat the model as a
failure requiring architectural rescue.

\appendix
\section{Reproduction and provenance}

The frozen implementation and result package were first committed at
\texttt{3d9af67}; the full source commit is recorded in the checkpoint manifest.
Committed evidence includes synthetic summaries, the K=128 stress, real config
and metrics, per-topic chemical scores, training history, theta support,
duplicate-component diagnostics, fragment/loss balance, top words, environment,
validation-access audit and provenance.

Verify the committed checkpoint without training:
\begin{verbatim}
conda run -n ms2lda-neural python \
  scripts/verify_routing_etm_checkpoint.py
\end{verbatim}

On the original host, verify every immutable validation input and retained large
model artifact:
\begin{verbatim}
conda run -n ms2lda-neural python \
  scripts/verify_routing_etm_checkpoint.py \
  --verify-inputs --verify-local-artifacts --require-external
\end{verbatim}

The full local check completes 78 integrity and cross-file consistency checks.
Exact real replay commands are in
\path{research/etm_ecrtm_msnlib/local_results/20260830_routing_etm/README.md}.
The machine-readable source of hashes, metrics, comparator values and replay
locations is the neighboring \path{checkpoint_manifest.json}. Large weights,
NumPy arrays, FAISS indexes, databases and raw MSnLib assets are retained outside
Git and identified by path, byte size and SHA-256.

\section{Frozen diagnostic settings}

The reporting contract uses mean topic usage 0.0005, duplicate-component cosine
thresholds 0.95/0.99/0.999, top 20 words, catastrophic strict-component fraction
0.5, fragment-mass extremes below 0.1 or above 0.9, and spectrum membership
threshold 0.5. These are frozen reporting and failure-detection settings.

\begin{thebibliography}{99}
\bibitem{vanderhooft2016}
J. J. J. van der Hooft et al. ``Topic modeling for untargeted substructure
exploration in metabolomics.'' \emph{Proceedings of the National Academy of
Sciences} 113, 13738--13743 (2016).
\url{https://doi.org/10.1073/pnas.1608041113}

\bibitem{torresortega2026}
M. Torres Ortega et al. ``Large-scale discovery and annotation of substructure
patterns in mass spectrometry profiles.'' \emph{Nature Communications} 17,
8350 (2026). \url{https://doi.org/10.1038/s41467-026-75038-0}

\bibitem{brungs2025}
C. Brungs et al. ``MS$^{n}$Lib: efficient generation of open multi-stage
fragmentation mass spectral libraries.'' \emph{Nature Methods} 22, 2028--2031
(2025). \url{https://doi.org/10.1038/s41592-025-02813-0}

\bibitem{dieng2020}
A. B. Dieng, F. J. R. Ruiz, and D. M. Blei. ``Topic Modeling in Embedding
Spaces.'' \emph{Transactions of the Association for Computational Linguistics}
8, 439--453 (2020). \url{https://doi.org/10.1162/tacl_a_00325}

\bibitem{peters2019}
B. Peters, V. Niculae, and A. F. T. Martins. ``Sparse Sequence-to-Sequence
Models.'' \emph{Proceedings of ACL 2019}, 1504--1519 (2019).
\url{https://doi.org/10.18653/v1/P19-1146}

\bibitem{wu2023}
X. Wu, X. Dong, T. Nguyen, and A. T. Luu. ``Effective Neural Topic Modeling
with Embedding Clustering Regularization.'' \emph{Proceedings of the 40th
International Conference on Machine Learning}, 2023.
\url{https://proceedings.mlr.press/v202/wu23c.html}

\bibitem{msnlibdata}
J. Dietrich, J. Wandy, L. R. Torres Ortega, and J. J. J. van der Hooft.
\emph{vdhooftcompmet/MS2LDA: Paper---Data and Models}. Zenodo (2026).
\url{https://doi.org/10.5281/zenodo.20179680}
\end{thebibliography}

\end{document}
